import { SerialDeviceResponse } from './serial-device-response';

describe('SerialDeviceResponse', () => {
  it('should be defined', () => {
    expect(new SerialDeviceResponse()).toBeDefined();
  });
});
