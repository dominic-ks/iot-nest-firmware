import { DeviceInterfaceClassDefinition } from './device-interface-class-definition';

describe('DeviceInterfaceClassDefinition', () => {
  it('should be defined', () => {
    expect(new DeviceInterfaceClassDefinition()).toBeDefined();
  });
});
